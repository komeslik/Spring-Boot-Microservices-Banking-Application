/**
 * Feature Flags
 *
 * Toggle features on/off by changing the boolean values below.
 * These flags control visibility and availability of specific features
 * across the frontend application.
 */

const featureFlags = {
  /** Show the Demo tab in the sidebar navigation */
  ENABLE_DEMO_TAB: true,

  /** Show the Send Money component inside the Demo tab dashboard */
  ENABLE_SEND_MONEY: true,
}

export default featureFlags
